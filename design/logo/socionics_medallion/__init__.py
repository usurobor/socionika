"""Socionics medallion: IR + compiler + executors.

Pure-Python plan/IR/compiler layer with two executor implementations:
StubExecutor (CadQuery-free) and CadQueryExecutor (lazy CadQuery import).
"""

from __future__ import annotations

__version__ = "0.1.0"
